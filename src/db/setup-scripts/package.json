
{
  "name": "sales-performance-db-setup",
  "version": "1.0.0",
  "description": "DB 초기화 및 마이그레이션 스크립트",
  "scripts": {
    "create-env": "node create-env-file.js",
    "create-db": "node create-db.js",
    "init-db": "node init-db.js",
    "migrate-data": "node migrate-data.js",
    "setup-all": "npm run create-env && npm run create-db && npm run init-db && npm run migrate-data"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.5"
  }
}
